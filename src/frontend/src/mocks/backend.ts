import type { backendInterface, Product, Order, Review, CartItem, Coupon, FlashSale, B2BInquiry, CouponValidation, AdminStats, StripeSessionStatus, TransformationOutput, UserRole } from "../backend";
import { OrderStatus, PaymentMethod } from "../backend";

const sampleProduct1: Product = {
  id: BigInt(1),
  featured: true,
  bundleIds: [],
  name: "Organic Ashwagandha Powder",
  ratings: 4.8,
  description: "Premium Ashwagandha root powder, traditionally sourced for stress relief, immunity boost, and vitality. 100% organic and pure.",
  stock: BigInt(100),
  imageKey: "organic_ashwagandha_forestheals_1",
  imageUrl: "/assets/products/organic_ashwagandha_forestheals_1-019d9662-2fc6-7459-b0c5-2c13876dc59e.jpg",
  discount: BigInt(10),
  category: "Ayurvedic Powders",
  price: BigInt(299),
  reviewCount: BigInt(48),
};

const sampleProduct2: Product = {
  id: BigInt(2),
  featured: true,
  bundleIds: [],
  name: "Organic Moringa Powder",
  ratings: 4.7,
  description: "Nature's superfood — packed with vitamins, minerals, and antioxidants. Ideal for daily nutrition and energy.",
  stock: BigInt(80),
  imageKey: "organic_moringa_forestheals_1",
  imageUrl: "/assets/products/organic_moringa_forestheals_1-019d9662-2c82-721a-8a13-e9acf8587930.jpg",
  discount: BigInt(0),
  category: "Ayurvedic Powders",
  price: BigInt(249),
  reviewCount: BigInt(32),
};

const sampleProduct3: Product = {
  id: BigInt(3),
  featured: true,
  bundleIds: [],
  name: "Brahmi Powder",
  ratings: 4.6,
  description: "Ancient brain tonic for memory, focus, and mental clarity. Pure and finely ground Brahmi leaves.",
  stock: BigInt(60),
  imageKey: "brahmi_forestheals_1",
  imageUrl: "/assets/products/brahmi_forestheals_1-019d9662-2c41-75bc-a5b8-eb510ab47ee9.jpg",
  discount: BigInt(5),
  category: "Ayurvedic Powders",
  price: BigInt(199),
  reviewCount: BigInt(21),
};

const sampleProduct4: Product = {
  id: BigInt(4),
  featured: false,
  bundleIds: [],
  name: "Triphala Churan",
  ratings: 4.9,
  description: "The classic Ayurvedic digestive blend of Amalaki, Bibhitaki, and Haritaki for gut health and detox.",
  stock: BigInt(90),
  imageKey: "triphala_churan_forestheals_1",
  imageUrl: "/assets/products/triphala_churan_forestheals_1-019d9662-2c85-7188-a222-c19b81ea8ecd.jpg",
  discount: BigInt(0),
  category: "Ayurvedic Powders",
  price: BigInt(179),
  reviewCount: BigInt(67),
};

const sampleProduct5: Product = {
  id: BigInt(5),
  featured: true,
  bundleIds: [],
  name: "Organic Amla Powder",
  ratings: 4.7,
  description: "Rich in Vitamin C, supports immunity, hair growth, and skin radiance. 100% pure Indian Gooseberry.",
  stock: BigInt(120),
  imageKey: "organic_amla_powder_forestheals_1",
  imageUrl: "/assets/products/organic_amla_powder_forestheals_1-019d9662-2c78-73db-94e2-e64ae483c495.jpg",
  discount: BigInt(15),
  category: "Ayurvedic Powders",
  price: BigInt(149),
  reviewCount: BigInt(54),
};

const sampleProduct6: Product = {
  id: BigInt(6),
  featured: false,
  bundleIds: [],
  name: "Organic Neem Powder",
  ratings: 4.5,
  description: "Purifying neem powder for skin, blood purification, and antibacterial benefits.",
  stock: BigInt(75),
  imageKey: "organic_neem_powder_forestheals_1",
  imageUrl: "/assets/products/organic_neem_powder_forestheals_1-019d9662-2c74-7308-abad-c167097c463a.jpg",
  discount: BigInt(0),
  category: "Ayurvedic Powders",
  price: BigInt(129),
  reviewCount: BigInt(29),
};

const sampleReview: Review = {
  id: BigInt(1),
  verified: true,
  userId: "2vxsx-fae" as unknown as import("@icp-sdk/core/principal").Principal,
  createdAt: BigInt(Date.now() * 1_000_000),
  text: "Absolutely love this product! I've been using it for 3 months and the quality is outstanding. Truly from the forest to my home.",
  productId: BigInt(1),
  approved: true,
  rating: BigInt(5),
};

const sampleOrder: Order = {
  id: BigInt(1),
  status: OrderStatus.delivered,
  paymentMethod: PaymentMethod.cod,
  userId: "2vxsx-fae" as unknown as import("@icp-sdk/core/principal").Principal,
  discountAmount: BigInt(0),
  createdAt: BigInt(Date.now() * 1_000_000),
  totalAmount: BigInt(598),
  address: {
    street: "123 Eco Street",
    country: "India",
    city: "Mumbai",
    postalCode: "400001",
    state: "Maharashtra",
  },
  items: [{ productId: BigInt(1), quantity: BigInt(2), price: BigInt(299) }],
};

const sampleCartItem: CartItem = {
  productId: BigInt(1),
  quantity: BigInt(1),
  price: BigInt(299),
};

const sampleFlashSale: FlashSale = {
  id: BigInt(1),
  startTime: BigInt(Date.now() * 1_000_000 - 3600_000_000_000),
  active: true,
  endTime: BigInt(Date.now() * 1_000_000 + 86400_000_000_000),
  discountPercent: BigInt(20),
  productId: BigInt(2),
};

export const mockBackend: backendInterface = {
  _initializeAccessControl: async () => undefined,
  addReview: async (_input) => sampleReview,
  addToCart: async () => undefined,
  addToWishlist: async () => undefined,
  approveReview: async () => true,
  assignCallerUserRole: async () => undefined,
  clearCart: async () => undefined,
  createCheckoutSession: async () => "cs_test_mock_session_id",
  createCoupon: async (input) => ({
    active: true,
    expiryDate: input.expiryDate,
    code: input.code,
    usedCount: BigInt(0),
    discountPercent: input.discountPercent,
    maxUses: input.maxUses,
  }),
  createFlashSale: async (input) => ({
    id: BigInt(1),
    startTime: input.startTime,
    active: true,
    endTime: input.endTime,
    discountPercent: input.discountPercent,
    productId: input.productId,
  }),
  createOrder: async (_input) => sampleOrder,
  createProduct: async (input) => ({
    id: BigInt(100),
    featured: input.featured,
    bundleIds: input.bundleIds,
    name: input.name,
    ratings: 0,
    description: input.description,
    stock: input.stock,
    imageKey: input.imageKey,
    imageUrl: input.imageUrl,
    discount: input.discount,
    category: input.category,
    price: input.price,
    reviewCount: BigInt(0),
  }),
  deleteProduct: async () => true,
  deleteReview: async () => true,
  endFlashSale: async () => true,
  getActiveFlashSales: async () => [sampleFlashSale],
  getAdminStats: async (): Promise<AdminStats> => ({
    totalProducts: BigInt(20),
    totalOrders: BigInt(157),
    totalRevenue: BigInt(47350),
  }),
  getCallerUserRole: async (): Promise<UserRole> => "user" as UserRole,
  getCart: async () => [sampleCartItem],
  getOrder: async () => sampleOrder,
  getProduct: async () => sampleProduct1,
  getProductReviews: async () => [sampleReview, {
    ...sampleReview,
    id: BigInt(2),
    text: "Excellent quality, fast delivery. The packaging is eco-friendly and the product feels very authentic.",
    rating: BigInt(5),
  }],
  getRecommendations: async () => [sampleProduct1, sampleProduct3, sampleProduct5],
  getStripeSessionStatus: async (): Promise<StripeSessionStatus> => ({
    __kind__: "completed",
    completed: { response: "success" },
  }),
  getWishlist: async () => [BigInt(2), BigInt(4)],
  isAdmin: async () => false,
  isCallerAdmin: async () => false,
  isStripeConfigured: async () => true,
  listAllOrders: async () => [sampleOrder],
  listB2BInquiries: async (): Promise<B2BInquiry[]> => [{
    id: BigInt(1),
    contactName: "Rajesh Kumar",
    createdAt: BigInt(Date.now() * 1_000_000),
    email: "rajesh@business.com",
    message: "Interested in bulk order of bio-coal for export.",
    productInterest: "Bio-Coal",
    companyName: "GreenEnergy Exports Pvt Ltd",
    quantity: "500 MT",
    phone: "+91 9876543210",
  }],
  listBundles: async () => [sampleProduct1, sampleProduct5],
  listCoupons: async (): Promise<Coupon[]> => [{
    active: true,
    expiryDate: BigInt(Date.now() * 1_000_000 + 30 * 86400_000_000_000),
    code: "FOREST10",
    usedCount: BigInt(12),
    discountPercent: BigInt(10),
    maxUses: BigInt(100),
  }],
  listEmails: async () => ["subscriber@example.com", "fan@eco.org"],
  listFeaturedProducts: async () => [sampleProduct1, sampleProduct2, sampleProduct3, sampleProduct5],
  listProducts: async () => [sampleProduct1, sampleProduct2, sampleProduct3, sampleProduct4, sampleProduct5, sampleProduct6],
  listUserOrders: async () => [sampleOrder],
  redeemCoupon: async () => true,
  removeFromCart: async () => undefined,
  removeFromWishlist: async () => undefined,
  seedProducts: async () => undefined,
  setStripeConfiguration: async () => undefined,
  setUserRole: async () => undefined,
  submitB2BInquiry: async (input) => ({
    id: BigInt(2),
    contactName: input.contactName,
    createdAt: BigInt(Date.now() * 1_000_000),
    email: input.email,
    message: input.message,
    productInterest: input.productInterest,
    companyName: input.companyName,
    quantity: input.quantity,
    phone: input.phone,
  }),
  subscribeEmail: async () => true,
  transform: async (input): Promise<TransformationOutput> => ({
    status: BigInt(200),
    body: input.response.body,
    headers: input.response.headers,
  }),
  updateCartQuantity: async () => undefined,
  updateOrderStatus: async () => true,
  updateProduct: async () => true,
  validateCoupon: async (): Promise<CouponValidation> => ({
    valid: true,
    discountPercent: BigInt(10),
    message: "Coupon applied successfully!",
  }),
  verifyAdminCredentials: async () => false,
  getTeamMembers: async () => [],
  addTeamMember: async (member) => member,
  updateTeamMember: async (_id, member) => member,
  deleteTeamMember: async () => false,
  getAdminTasks: async () => [],
  createAdminTask: async (task) => task,
  updateAdminTask: async (_id, task) => task,
  deleteAdminTask: async () => false,
  completeAdminTask: async () => false,
  getExpenses: async () => [],
  addExpense: async (expense) => expense,
  updateExpense: async (_id, expense) => expense,
  deleteExpense: async () => false,
  getExpensesByCategory: async () => [],
  getBlogPosts: async () => [],
  createBlogPost: async (post) => post,
  updateBlogPost: async (_id, post) => post,
  deleteBlogPost: async () => false,
  publishBlogPost: async () => false,
  getAdminNotifications: async () => [],
  markNotificationRead: async () => false,
  clearAllNotifications: async () => undefined,
  getStoreSettings: async () => null,
  saveStoreSettings: async (settings) => settings,
  getAdminCoupons: async () => [],
  createAdminCoupon: async (coupon) => coupon,
  updateAdminCoupon: async (_id, coupon) => coupon,
  deleteAdminCoupon: async () => false,
  toggleCouponActive: async () => false,
  getInventoryItems: async () => [],
  upsertInventoryItem: async (item) => item,
  updateInventoryStock: async () => null,
  getLowStockItems: async () => [],
  getOutOfStockItems: async () => [],
  getAnalyticsSummary: async () => ({
    totalOrders: BigInt(0),
    totalExpenses: BigInt(0),
    totalRevenue: BigInt(0),
    totalCustomers: BigInt(0),
    avgOrderValue: BigInt(0),
    netProfit: BigInt(0),
  }),
};
